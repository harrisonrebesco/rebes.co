---
export interface Props {
  type: "BlogPosting" | "Person" | "WebSite";
  data: any;
}

const { type, data } = Astro.props;

let structuredData = {};

if (type === "BlogPosting") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "BlogPosting",
    headline: data.title,
    description: data.description,
    author: {
      "@type": "Person",
      name: data.author || "Harry Rebesco",
      url: "https://harry.rebes.co/about",
    },
    datePublished: data.pubDatetime.toISOString(),
    dateModified: data.modDatetime?.toISOString() || data.pubDatetime.toISOString(),
    publisher: {
      "@type": "Person",
      name: "Harry Rebesco",
      logo: {
        "@type": "ImageObject",
        url: "https://harry.rebes.co/peter-avatar.jpg",
      },
    },
    mainEntityOfPage: {
      "@type": "WebPage",
      "@id": data.url,
    },
    image: data.ogImage || "https://harry.rebes.co/peter-avatar.jpg",
    articleSection: data.tags?.[0] || "Technology",
    keywords: data.tags?.join(", ") || "",
    wordCount: data.wordCount,
    timeRequired: data.readingTime,
  };
} else if (type === "Person") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "Person",
    name: "Harry Rebesco",
    url: "https://harry.rebes.co",
    image: "https://harry.rebes.co/peter-avatar.jpg",
    sameAs: [
      "https://github.com/harrisonrebesco",
      "https://www.linkedin.com/in/harrisonrebesco/",
    ],
    jobTitle: "Software Engineer",
    description:
      "AI-powered tools from Swift roots to web frontiers. Every commit lands on GitHub for you to fork & remix.",
  };
} else if (type === "WebSite") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "WebSite",
    name: "Harry Rebesco",
    url: "https://harry.rebes.co",
    description:
      "AI-powered tools from Swift roots to web frontiers. Every commit lands on GitHub for you to fork & remix.",
    author: {
      "@type": "Person",
      name: "Harry Rebesco",
    },
    potentialAction: {
      "@type": "SearchAction",
      target: "https://harry.rebes.co/search?q={search_term_string}",
      "query-input": "required name=search_term_string",
    },
  };
}
---

<script type="application/ld+json" set:html={JSON.stringify(structuredData)} />
